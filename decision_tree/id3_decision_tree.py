#encoding=utf-8
class ID3DecisionTree(object):
    def __init__(self, inputs, labels, info_gain_thres):
        self.training_inputs = inputs
        self.training_labels = labels
        self.info_gain_thres = info_gain_thres

    def train(self):


    def predict(self, input):
        
    